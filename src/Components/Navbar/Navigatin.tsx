import NavLinks from "./NavLinks";
import './navbar.css';

const Navigation = () => {
    return (
        <nav className="navi">
            <NavLinks />
        </nav>
    );
}

export default Navigation;